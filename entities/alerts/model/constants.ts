// it's called "default", but is actually "dense"
export const DEFAULT_ROW_STYLE = "default";

// Default for the dispose-on-new-alert toggle in the dismiss/status/assign
// modals: enrichments are kept when the alert re-fires unless the user opts in.
export const DEFAULT_DISPOSE_ON_NEW_ALERT = false;
